<?php

// login.php {error | null | type=new name login pass | type=login login pass} => redirect (login.php {error}, tasks.php) or show error

include_once("functions.php");

if (true) {
    include_once("templates/login.html");
}